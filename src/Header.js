import React from "react";

const Header = () => {
  return (
    <header className="header">
      <button>Users</button>
      <button>Posts</button>
      <button>Comments</button>
    </header>
  );
};

export default Header;
